/*import React from 'react'

function Car(props) {
    const { name, model, year, partner } = props.carnob
    console.log(props)
    return (
        <div>
            <div>{name}</div>
            <div>{model}</div>
            <div>{year}</div>
            <div>{partner}</div>
        </div>
    )
}

export default Car*/
